# Course upgrade — gas-mastery · 2026-05-31

สรุปการปรับปรุงคอร์ส **Google Apps Script & Web App — Mastery Course** (`gas-mastery`)
เป้าหมาย: เป็นมิตรกับมือใหม่เริ่มจาก 0 · มีไฟล์ให้คัดลอกครบทั้งหลักสูตร · ทำตามทีละขั้นแบบเห็นภาพ

> เนื้อหาทั้งหมดอยู่ใน **Supabase DB** (ตาราง `lessons`, `course_modules`) ของ project `hgjrxixbdmklwzcfyrxm`
> คอร์สยัง `status = draft` — นักเรียนยังไม่เห็นจนกว่าจะ publish
> backup เป็นไฟล์ SQL: รัน `node course-content/export-from-db.mjs`

---

## 1. Capstone — เปลี่ยนเป็น "copy แล้วเข้าใจ" + ไฟล์เต็มรันได้

แนวการสอนใหม่ทุก capstone: **คัดลอกไฟล์ก่อน → อธิบายหลักการ → ลงมือทำทีละขั้น (มีช่องใส่รูป) → ไฟล์เต็ม**
โค้ด **self-contained ไม่มี ID ให้แก้** (`SpreadsheetApp.getActive()`, `CalendarApp.getDefaultCalendar()`, `ScriptApp.getService().getUrl()`)

| บท | lesson id | ไฟล์ที่ copy ได้ |
|---|---|---|
| 12.2 Project A — Event Registration | `19992026-dc5d-4471-ab94-af55e7e0591a` | appsscript.json · Code.gs · Index · CheckIn · Dashboard |
| 12.3 Project B — Order Management | `24426e66-562f-4a73-a750-74d2738d4ba9` | appsscript.json · Code.gs · Index · Admin |
| 12.4 Project C — Appointment Booking | `feff485c-b8da-4a14-9075-a1737209800c` | appsscript.json · Code.gs · Index · Staff |

ทุกตัวผ่าน generate → **adversarial verify** → fix (แก้จริง เช่น OrderID/BookingID ไม่ชนเมื่อลบแถว, day-of-week TZ-safe, scope ตรงกับโค้ด, dead-link handling)
หมายเหตุ: A เปลี่ยน flow จาก Google Form + trigger → ฟอร์มในเว็บแอป (`google.script.run`) เพื่อให้ "ทำสำเนา" ติดมาครบ

## 2. Module 0 — เตรียมพร้อมก่อนเริ่ม (ใหม่)
`course_modules.sort_order = 0` · id `09b33e69-4001-4bb1-b4f0-4e3c692f9f58` · 4 บท (core, free preview)
1. คอร์สนี้สอนอะไร + ต้องเตรียมอะไรบ้าง
2. ทำสำเนาไฟล์เป็น (Make a copy) + รู้จัก Google Drive
3. ทัวร์หน้าจอ Apps Script editor — ปุ่มไหนทำอะไร
4. เช็กความพร้อม + เคล็ดลับเรียนให้รอด

## 3. Quiz — เติมทุกโมดูล
เพิ่ม quiz **45 บท · 180 ข้อ** ในโมดูล 2-11 (เฉพาะบทที่ยังไม่มี · ข้ามบทที่มี Self-check อยู่แล้ว)
รูปแบบตรงกับตัว render: `{ "q", "choices":[4], "correct": <index>, "explain" }`
M1 มีอยู่แล้ว · M12 (capstone) เว้นไว้เพราะใช้ Self-check แทน

## 4. core / quick-path
ติด `is_core` ให้โมดูล 5-11 (เดิม 0 ทั้งหมด) บทรากฐาน 1-2 บท/โมดูล:
M5 x-model · M6 Session.getActiveUser + Role-based · M7 Time-based Triggers + UrlFetchApp · M8 PDF generation · M10 clasp setup · M11 LINE Messaging API setup

## 5. รูปภาพ — placeholder → ช่องใส่รูป
แปลง `![..](https://placeholder.kp/..)` ที่เสีย (20 จุด · 10 บท) → callout `> 📷 **[ใส่รูปตรงนี้]** — <คำอธิบาย>`
เจ้าของคอร์สเติมรูปจริงที่ marker เหล่านี้ได้เลย · ตอนนี้มี image slot รวม 17 บท

---

## เหลือให้เจ้าของคอร์สทำ
- [ ] publish Google template ของ A/B/C → ดู [docs/instructor/capstone-templates-setup.md](../docs/instructor/capstone-templates-setup.md) แล้วส่งลิงก์ `/copy` กลับมาใส่ "ขั้นที่ 0"
- [ ] เติมรูปจริงที่ช่อง `📷 [ใส่รูปตรงนี้]`
- [ ] (ออปชัน) อัปเดต Capstone Overview (12.1) ให้เข้ากับแนว copy-first
- [ ] รีวิว + `publish` คอร์ส (เปลี่ยน `courses.status` จาก draft)
